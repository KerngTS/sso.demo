@page "/Admin/Users/Create"
@model AuthServer.Pages.Admin.Users.CreateModel
@attribute [Microsoft.AspNetCore.Authorization.Authorize(Roles = "admin,UserManager")]
@{
    ViewData["Title"] = "新增用戶";
}

<h2>@ViewData["Title"]</h2>

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="alert alert-danger">@Model.ErrorMessage</div>
}

<form method="post">
    <div class="row mb-3">
        <div class="col-md-8">
            <label asp-for="Input.Email" class="form-label">電子郵件（同時作為登入帳號）</label>
            <input asp-for="Input.Email" class="form-control" type="email" placeholder="user@example.com" />
            <span asp-validation-for="Input.Email" class="text-danger"></span>
        </div>
        <div class="col-md-4">
            <label asp-for="Input.Password" class="form-label">密碼</label>
            <input asp-for="Input.Password" class="form-control" type="password" />
            <span asp-validation-for="Input.Password" class="text-danger"></span>
        </div>
    </div>

    <h5 class="mt-3">角色指派</h5>
    <div class="mb-3">
        @foreach (var role in Model.AvailableRoles)
        {
            <div class="form-check form-check-inline">
                <input class="form-check-input" type="checkbox" name="SelectedRoles" value="@role" id="r_@role" />
                <label class="form-check-label" for="r_@role">@role</label>
            </div>
        }
    </div>

    <button type="submit" class="btn btn-primary">建立</button>
    <a class="btn btn-secondary" asp-page="Index">取消</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
